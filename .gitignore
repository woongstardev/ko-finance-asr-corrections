__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
build/
.DS_Store
tmp/
scratch/
benchmark/predictions/
# person info — list lives upstream (private); local legacy copy stays untracked (task 006)
scripts/person-exclusions.txt

# Internal working documents. These describe upstream internals and operational
# detail, which AGENTS.md forbids committing here; they are kept outside this
# repository and may be symlinked back in for local work.
CLAUDE.md
tasks
docs/GGULMUSE-CONTEXT.md
